class Book {
    #private_variable;
    _protected_variable;

    constructor(id, title, author, pageCount) {
        this.id = id;
        this.title = title;
        this.author = author;
        this.pageCount = pageCount;
    }
}